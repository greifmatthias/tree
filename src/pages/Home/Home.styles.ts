import styled from '@emotion/styled';

import { IconButton } from 'components';

const AddButton = styled(IconButton)({
  position: 'absolute',
  bottom: 24,
  right: 24,
});

export default {
  AddButton,
};
